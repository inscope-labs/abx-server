<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        Overflow menu items for compliance pages. All items are showAsAction="never"
        so they appear in the overflow (three-dot) menu.
    -->
    <item
        android:id="@+id/menu_about"
        android:title="@string/menu_about"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_privacy_policy"
        android:title="@string/menu_privacy_policy"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_delete_data"
        android:title="@string/menu_delete_data"
        app:showAsAction="never" />

</menu>
